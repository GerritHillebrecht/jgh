import { SortToppingPipe } from './sort-topping.pipe';

describe('SortToppingPipe', () => {
  it('create an instance', () => {
    const pipe = new SortToppingPipe();
    expect(pipe).toBeTruthy();
  });
});
