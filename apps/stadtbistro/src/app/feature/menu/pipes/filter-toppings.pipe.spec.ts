import { FilterToppingsPipe } from './filter-toppings.pipe';

describe('FilterToppingsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterToppingsPipe();
    expect(pipe).toBeTruthy();
  });
});
