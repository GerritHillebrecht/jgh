import { ToppingCategoryPipe } from './topping-category.pipe';

describe('ToppingCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ToppingCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
